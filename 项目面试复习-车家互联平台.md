# 车家互联平台项目面试复习

## 1. 项目一句话介绍

这是一个基于 `Vue 3 + TypeScript + Pinia + FastAPI` 的车家互联智能控制平台，主要面向家庭设备和车辆设备的统一管理，支持首页总览、家居控制、车辆控制、3D 车辆展示，以及 AI 助手对话与智能建议能力。

---

## 2. 我在项目中的核心工作

1. 负责首页总览、家居控制、车辆控制等核心页面开发，完成灯光、窗帘、空调、车辆空调、车门、充电状态等模块的展示、交互与联动。
2. 使用 `Three.js` 封装 3D 车辆展示组件，完成 GLB 模型加载、HDR 环境接入、相机适配、阴影与地面效果处理，并支持业务页面复用。
3. 基于 `Pinia` 管理环境与车辆状态，完成前后端接口联调，处理状态获取、局部更新、失败回滚与页面同步逻辑，保证前后端数据一致性。
4. 设计并接入 AI 助手能力，基于 `SSE` 实现流式对话输出，支持 Markdown 富文本渲染与安全处理，并将助手抽屉挂载到全局布局层，提升多页面智能交互体验。

---

## 3. 技术栈

- 前端：`Vue 3`、`TypeScript`、`Pinia`、`Vuetify`
- 3D：`Three.js`
- 后端：`FastAPI`、`Pydantic`
- AI：大模型接口、`SSE` 流式返回
- 协议与渲染：`Markdown`、受控 HTML 渲染

---

## 4. 项目结构怎么讲

### 4.1 前端

- `src/views/dashboard/overview.vue`
  - 首页总览
  - 状态卡片、告警、AI 建议、快捷场景入口
- `src/stores/useEnvironmentStore.ts`
  - 家居设备状态管理
- `src/stores/useVehicleStore.ts`
  - 车辆状态管理
- `src/stores/useDashboard.ts`
  - 首页聚合数据与 AI 建议拉取
- `src/stores/useAiAssistant.ts`
  - AI 助手消息流、状态管理、异常收尾
- `src/api/ai.ts`
  - AI 普通接口与流式 SSE 接口封装
- `src/components/AiAssistantDrawer/index.vue`
  - AI 助手抽屉
  - Markdown 渲染

### 4.2 后端

- `app/routers/environment.py`
  - 家居设备状态查询与修改
- `app/routers/vehicle.py`
  - 车辆状态查询与修改
- `app/routers/dashboard.py`
  - 首页聚合数据
- `app/routers/ai.py`
  - AI 对话
  - AI 建议
  - 流式 SSE 返回
- `app/state.py`
  - 当前项目阶段使用内存态保存设备与车辆状态

---

## 5. 我做的几个重点模块

### 5.1 首页总览

首页不是简单的信息堆叠，而是把：

- 当前家庭状态
- 当前车辆状态
- 高优先级事项
- AI 建议
- 快捷场景入口

整合成一个总览面板，让用户在首页就能完成查看和操作。

### 5.2 Pinia 状态管理

我把环境状态和车辆状态拆开管理：

- `useEnvironmentStore`
- `useVehicleStore`

这样做的好处：

1. 页面之间可以共享状态
2. 更新逻辑可以统一沉淀到 store
3. 接口请求、乐观更新、失败回滚都更好维护

比如车辆充电、车内空调、窗帘、灯光这些更新，都是先更新前端状态，再请求后端，失败后回滚。

### 5.3 3D 车辆展示

我用 `Three.js` 做了车辆 3D 展示组件，支持：

- GLB 模型加载
- HDR 环境贴图
- 相机视角适配
- 地面与阴影效果
- 组件复用

这个点在面试里可以强调成：我不仅做业务页面，也能处理偏图形和可视化的前端能力。

### 5.4 AI 助手

这个模块是我这次项目里比较有亮点的部分。

我做了三件事：

1. 前后端 AI 对话链路接通
2. 基于 SSE 实现流式输出
3. 前端支持 Markdown 渲染和异常收尾

---

## 6. SSE 流式对话怎么讲

### 6.1 为什么用 SSE

因为 AI 回复是逐段生成的，如果等完整文本都返回再展示，用户体验会比较差。

所以我用了 `SSE`：

- 后端边生成边 `yield`
- 前端边接收边显示

用户能明显感受到“AI 在实时回复”。

### 6.2 前端做了什么

前端在 `src/api/ai.ts` 中：

1. 发起流式请求
2. 从 `ReadableStream` 里持续读取数据
3. 用 `buffer` 处理半截事件块
4. 按 `\n\n` 拆分 SSE block
5. 解析 `start / chunk / done / error`
6. 通过回调把数据交给 store

### 6.3 store 做了什么

在 `useAiAssistant.ts` 里：

- 先插入一条空的 assistant 消息
- `onChunk` 时不断追加内容
- `onDone` 时收尾并保存 commands / commandResults
- `onError` 时保留已收到内容，并补一条中断提示

### 6.4 这个设计的好处

1. 用户体验更好
2. API 层、状态层、UI 层职责清晰
3. 支持后续扩展更复杂的 AI 交互

---

## 7. Markdown 渲染怎么讲

### 7.1 为什么做 Markdown 渲染

因为 AI 回复不只是普通文本，经常会包含：

- 标题
- 列表
- 强调
- 行内代码
- 代码块

如果前端直接按纯文本显示，这些结构就浪费了。

### 7.2 我做了什么

在 `AiAssistantDrawer/index.vue` 里实现了受控 Markdown 渲染，支持：

- 标题
- 列表
- 引用
- 行内代码
- 代码块
- 粗体 / 斜体 / 删除线

### 7.3 安全怎么处理

我没有直接把模型输出原样塞进 `v-html`，而是：

1. 先做 `escapeHtml`
2. 再按受控规则把允许的 Markdown 语法转换成有限 HTML
3. 避免原始 HTML 直接进入页面，降低 XSS 风险

### 7.4 这块面试怎么说

可以说：

> 我做的不是简单的字符串替换，而是把 AI 内容输出和展示分层，既保留 Markdown 的结构化表达，又考虑了前端渲染安全。

---

## 8. 首页 AI 建议这块怎么讲

这里其实有一个很好的工程判断点。

我一开始接的是 AI 建议接口，但后来发现：

- 首页这种“可直接执行的动作建议”
- 更强调稳定性和一致性
- 不适合完全依赖大模型自由生成

所以这个点我可以在面试里讲成一个设计思考：

> 聊天助手适合用大模型，因为它强调自然交互；但首页动作建议更偏规则引擎，因为它要求稳定、可控、可解释，不能出现设备已经关闭却还继续建议关闭的情况。

这个回答会让面试官觉得你不是只会接 API，而是在做工程取舍。

---

## 9. 这项目里我能重点展开的亮点

### 亮点 1：前后端联动完整

不是单纯写页面，而是把：

- 状态展示
- 设备更新
- 回滚
- 页面刷新
- AI 建议

整条链路打通了。

### 亮点 2：SSE 流式交互

这是比较典型、也比较加分的能力点，说明你不只是会调普通 REST 接口。

### 亮点 3：Markdown 渲染与安全

这能体现你对“AI 内容展示”这件事有工程意识，不只是能跑通功能。

### 亮点 4：能讲出规则与 AI 的边界

这是很加分的设计思维：

- 什么时候适合 AI
- 什么时候适合规则
- 为什么首页建议和聊天助手要分开考虑

---

## 10. 面试时可以这样自我表述

### 简短版

> 我在这个项目里主要负责首页总览、家居和车辆控制模块开发，也做了 AI 助手能力接入。除了基础页面和状态管理，我还完成了基于 SSE 的流式对话、Markdown 渲染，以及前后端状态同步和失败回滚这类偏工程化的部分。

### 展开版

> 这个项目是一个车家互联平台，前端用 Vue3、TypeScript、Pinia，后端是 FastAPI。我主要做了三块：第一块是首页总览和控制模块，把灯光、窗帘、空调、车门、充电这些状态和操作串起来；第二块是 Three.js 的 3D 车辆展示；第三块是 AI 助手能力，包括流式 SSE 对话、Markdown 渲染、前后端命令与状态联动。我在这个过程中比较关注的是工程上的完整性，比如状态一致性、失败回滚、展示安全，以及 AI 能力应该放在哪些场景更合适。

---

## 11. 高频面试追问与回答思路

### Q1：为什么选 Pinia？

答：

- Vue3 官方推荐
- 类型支持更自然
- 适合状态共享
- 逻辑能从组件中抽离出来

### Q2：为什么用 SSE，不用 WebSocket？

答：

- 这里核心需求是服务端单向持续推送 AI 文本
- SSE 更轻量，协议简单
- 对当前场景足够
- 如果是双向实时协作/长连接指令同步，再考虑 WebSocket

### Q3：为什么做 Markdown 渲染？

答：

- AI 回复有结构化内容
- 纯文本会损失可读性
- Markdown 渲染可以提升展示效果
- 同时要考虑 XSS，所以不能直接原样 `v-html`

### Q4：为什么首页建议不适合完全交给 AI？

答：

- 首页建议是动作建议
- 要求稳定、可执行、可解释
- 大模型适合自然语言理解和生成
- 规则更适合做确定性决策

### Q5：你在项目里遇到过什么问题？

答题方向可以说：

1. SSE 流里 chunk 和业务事件边界不一致，需要用 buffer 处理半截数据
2. AI 助手异常时会重复追加中断提示，后面做了去重
3. 首页建议使用 LLM 时会出现状态已变化但建议未变化的问题，后面认识到规则和 AI 的边界

---

## 12. 临近面试前重点复习什么

如果时间不多，优先复习这 5 个点：

1. `Pinia` 状态管理和失败回滚
2. `SSE` 流式返回完整链路
3. Markdown 渲染与安全处理
4. 3D 车辆展示组件做了什么
5. 首页 AI 建议为什么更适合规则驱动

---

## 13. 最后给自己的提醒

面试时不要只说“我做了 AI 助手”。

要尽量讲成：

- 我解决了什么问题
- 为什么这么设计
- 中间遇到什么坑
- 我怎么权衡规则和 AI

这样会比单纯背技术名词更有说服力。

